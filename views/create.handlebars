<div class="container">
  <div class="row">
    <h3 class="m-4 font-weight-bold">創建餐廳資訊</h3>
  </div>
  <div class="row">
    <form action="/create" method="POST" style="width: 100%;">
      <div class="container">
        {{#if errorMsg}}

        <div class="row">
          <div class="col-12 alert alert-danger alert-dismissible fade show" role="alert">
            送出失敗: 請確認已經輸入必填項目 (店名、類別、評分、餐廳描述) 且評分只能輸入數字喔！
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
        </div>

        {{/if}}
        <div class="row mt-2">
          <label for="name" class="col-2 text-center pt-2">店名</label>
          <input type="text" class="form-control col-10" name="name" placeholder="*必填" value="{{restaurant.name}}">
        </div>
        <div class="row mt-2">
          <label for="name_en" class="col-2 text-center pt-2">英文店名</label>
          <input type="text" class="form-control col-10" name="name_en" value="{{restaurant.name_en}}">
        </div>
        <div class="row mt-2">
          <label for="category" class="col-2 text-center pt-2">類別</label>
          <input type="text" class="form-control col-10" name="category" placeholder="*必填"
            value="{{restaurant.category}}">
        </div>
        <div class="row mt-2">
          <label for="image" class="col-2 text-center pt-2">相片</label>
          <input type="text" class="form-control col-10" name="image" value="{{restaurant.image}}">
        </div>
        <div class="row mt-2">
          <label for="location" class="col-2 text-center pt-2">地點</label>
          <input type="text" class="form-control col-10" name="location" value="{{restaurant.location}}">
        </div>
        <div class="row mt-2">
          <label for="phone" class="col-2 text-center pt-2">電話</label>
          <input type="text" class="form-control col-10" name="phone" value="{{restaurant.phone}}">
        </div>
        <div class="row mt-2">
          <label for="google_map" class="col-2 text-center pt-2">Google Map 網址</label>
          <input type="text" class="form-control col-10" name="google_map" value="{{restaurant.google_map}}">
        </div>
        <div class="row mt-2">
          <label for="rating" class="col-2 text-center pt-2">評分</label>
          <input type="text" class="form-control col-10" name="rating" placeholder="*必填" value="{{restaurant.rating}}">
        </div>
        <div class="row mt-2">
          <label for="description" class="col-2 text-center pt-2">餐廳描述</label>
          <textarea type="text" class="form-control col-10" name="description"
            placeholder="*必填">{{restaurant.description}}</textarea>
        </div>
      </div>

      <div class="d-flex justify-content-end mt-4">
        <button type="submit" class="btn btn-primary">確定新增</button>
        <a href="/" class="btn btn-secondary ml-2">返回</a>
      </div>
    </form>
  </div>
</div>